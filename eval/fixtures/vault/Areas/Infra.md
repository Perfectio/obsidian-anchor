# Infrastructure

We run everything on a single Postgres instance for now; see [[Auth]] for the storage decision. ^infra1

The app is deployed on Fly.io. Database backups run nightly at 02:00 UTC. ^b7c3
